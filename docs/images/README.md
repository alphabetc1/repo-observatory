# Demo Screenshots

These are unmodified browser captures of the running application, using the recorded public GitHub fixtures in `tests/fixtures/`. They illustrate the default SGLang cache profile, not a live service or current upstream status. Fixture timestamps are adjusted by the browser test to keep entries visible in the time filters.

| Image | View |
| --- | --- |
| `desktop-en.png` | English desktop workspace |
| `desktop-zh.png` | Chinese desktop workspace |
| `detail-en.png` | Issue details and evidence |
| `mobile-390-en.png` | English mobile workspace |

To regenerate the captures, follow the browser verification commands in the root README. Review the resulting images in `artifacts/` before replacing these curated copies.

Only publish screenshots backed by public fixtures. Do not publish account pages, invitation links, tokens, private repository content, browser chrome, personal paths, or deployment details. Generated captures remain ignored; only these reviewed images belong in Git.
