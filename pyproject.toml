[project]
name = "repo-observatory"
version = "0.1.0"
description = "A configurable GitHub issue and pull request observatory"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
target-version = "py310"
